[project]
name = "finssim-isaaclab-project"
version = "0.1.0"
description = "Isaac Lab 3 tasks and runtime adapters for FinsSim."
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = ["numpy>=1.26,<3", "pytest>=8,<9", "ruff>=0.8,<1"]

[tool.ruff]
line-length = 100
target-version = "py312"
